import { TutorialScreen } from '../src/components/tutorial/TutorialScreen';

// How this works — a short orientation page reachable from Settings (and from
// the mobile drawer/More lists where the design places it).
export default function Tutorial() {
  return <TutorialScreen />;
}
