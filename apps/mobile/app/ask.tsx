import { AskScreen } from '../src/components/queue/AskScreen';

// Ask the team (#545) — the trainee's person-less questions, pushed from the ☰
// drawer. A full-timer answers from My Day.
export default function Ask() {
  return <AskScreen />;
}