{
  "name": "cisa-notification-functions",
  "private": true,
  "description": "Pushes every notification-bell entry to the recipient's registered devices (Expo + Web Push).",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/web-push": "^3.6.4",
    "typescript": "~5.8.2",
    "vitest": "^5.0.1"
  }
}
