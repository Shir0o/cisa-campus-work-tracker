{
  "rules": {
    ".read": false,
    ".write": false,

    "board_docs_rtdb": {
      "$docId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "bible_study_meetings_rtdb": {
      "$meetingId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
