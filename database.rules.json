{
  "rules": {
    ".read": false,
    ".write": false,

    "board_docs_rtdb": {
      "$docId": {
        ".read": "auth != null && (auth.token.admin === true || (auth.token.email === 'yilongwang05@gmail.com' && auth.token.email_verified === true))",
        ".write": "auth != null && (auth.token.admin === true || (auth.token.email === 'yilongwang05@gmail.com' && auth.token.email_verified === true))"
      }
    }
  }
}
